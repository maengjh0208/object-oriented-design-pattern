[project]
name = "object-oriented-design-pattern"
version = "0.1.0"
description = "객체지향 디자인 패턴 구현해보기"
readme = "README.md"
authors = [
    { name = "maengjh", email = "juhee.bda@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9",
    "ruff>=0.16.6",
]

[tool.uv]
package = false
